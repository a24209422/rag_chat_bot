# CLAUDE.md

專案本身的說明在 `README.md`（架構、為什麼這樣切、踩過的坑）。這一份只記
「在這個 repo 工作時一定要遵守的規矩」。

## 待修正清單（每次改動後都要做）

根目錄有一份 `待修正清單.md`。**只要一次改動之後留下任何「知道但現在不修」
的東西，就要寫進去**，包括：

- 過程中發現、但不屬於這次範圍的 bug
- 為了先動起來而留下的暫解、繞道、寫死的值
- 這次改動帶來的新風險：效能、相容性、需要資料遷移、需要重算索引
- 使用者提出、但當下沒有處理的疑慮
- 「我沒驗證過但懷疑會壞」的地方——標明未驗證，照樣寫

每一條至少要有：

```
### 編號 嚴重度 一句話症狀
**症狀**　使用者看到什麼
**原因**　檔案:行 + 為什麼會這樣
**重現**　最短的重現步驟或腳本；沒驗證過就寫「未驗證，推論自 …」
**修法方向**　不用寫完整解法，寫到「下次接手的人不必重新想」就夠
```

嚴重度分三級：`🔴 高`＝使用者會遇到而且沒有跡象；`🟡 中`＝條件湊齊才會遇到；
`⚪ 低`＝粗糙但不致命。**「沒有跡象」是升級的理由**——會當場報錯的問題比默默
給出錯答案的問題安全。

### 修好之後

**不要刪掉那一條。** 把標題用 `~~刪除線~~` 劃掉，整條移到「已處理」，並寫上
修它的 commit hash。保留是為了下次有人問「這個以前是不是遇過」時查得到，
也為了看得出同一個地方是不是一直出事。

### 幾條相關的規矩

- 這份清單是**現在的狀態**，不是歷史。歷史在 commit 訊息裡，所以清單不進
  版控（`.gitignore`），commit 訊息也不要引用它的行號。
- 不要為了讓清單看起來乾淨而不寫。寫進去是誠實，不是承認失敗；反過來說，
  修完一條就要真的劃掉，不然它會慢慢變成沒人看的噪音。
- 報告工作成果時，如果這次有新增條目，要在回覆裡提一句加了什麼，不要只是
  默默寫進檔案。

## 其他

- 註解與 commit 訊息用中文，跟現有的檔案一致；寫「為什麼」不要寫「做了什麼」。
- 改完跑 `python -m pytest -q` 與 `python -m ruff check .`；前端跑
  `npm run lint` 與 `npm run build`（在 `frontend/`）。pre-commit 也會擋。
- `shared/facets.py` 的分類規則改了之後，已經存起來的 facets 不會自己更新，
  要跑 `python -m tools.refacet`（jobs.json、兩個索引檔、登記簿都要）。
