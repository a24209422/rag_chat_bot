# 環境變數（含 API 金鑰，絕對不要提交）
.env
.env.local
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/
# 測試與 lint 的快取。這兩個工具會自己在目錄裡寫 .gitignore，
# 但那太隱晦了——明寫在這裡，看 .gitignore 就知道會產生什麼。
.pytest_cache/
.ruff_cache/

# 模型與快取
models/
*.gguf
.cache/
# 向量索引快取（可由 jobs.json 重算；gitignore 不吃行尾註解，註解要自己一行）
data/vecs_*.npz


# 編輯器 / 作業系統
.DS_Store
Thumbs.db
.tmp.drivedownload/
.tmp.driveupload/

# 個人開發筆記，留在本機不進版控（已從 git 歷史中移除）
地端改動.md
